{
  "name": "claude-html-to-pdf",
  "version": "2.0.0",
  "description": "Professional HTML to PDF conversion tool optimized for Claude AI presentations",
  "main": "index.html",
  "scripts": {
    "dev": "live-server . --port=8080 --host=localhost",
    "build": "echo 'Static site - no build required'",
    "start": "live-server . --port=8080",
    "test": "echo 'No tests configured'",
    "serve": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
